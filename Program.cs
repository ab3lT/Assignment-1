using System;

using static assignment_one.Palindrome;

namespace assignment_one
{
    public class Program
    {
        public static void Main(string[] args)
        {
            /* string console = string.Empty;
            do {
                
            }
            while (console == "exit" || console == "Exit");
            {
                 Menu.menu();
            }
            
            //Palindrome.palendrome(revString, input);
        */
                    Menu.menu(); 
            System.Console.WriteLine("-------------------------------------");
            System.Console.WriteLine("-------------------------------------");

            System.Console.WriteLine("-------------------------------------");

            Console.WriteLine("         The program excuted perfectly!! ");
            System.Console.WriteLine("-------------------------------------");
            System.Console.WriteLine("-------------------------------------");
            System.Console.WriteLine("-------------------------------------");
            System.Console.WriteLine("--------------------------------------");
            System.Console.WriteLine("       Thanks for running the app    ");
            System.Console.WriteLine("-------------------------------------");
            System.Console.WriteLine("-------------------------------------");

           
            
        }

        /* public static bool isPalendrome(string userInput)
        {
            for (int i = 0; i < (userInput.Length / 2); i++)
            {
                if (userInput[i] != userInput[userInput.Length - 1 - i])
                {
                    System.Console.WriteLine("False");
                    return false;
                }
            }
            System.Console.WriteLine("True");
            return true; */
        }
    }

